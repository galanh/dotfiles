# Mensajes de diarios en 3 idiomas
message(rep("*", 70))
message(sample(utils::read.delim("/home/gato/bin/texts/diario_de_ideas.txt", header = FALSE)$V2,1))
message(sample(utils::read.delim("/home/gato/bin/texts/daily_record.txt", header = FALSE)$V2,1))
message(sample(utils::read.delim("/home/gato/bin/texts/carnet_de_notes.txt", header = FALSE)$V2,1))
message(rep("*", 70))

# Force the default screen device to be a reasonably sized Cairo window
#options(device = function() cairo_x11(width = 7, height = 6))


